# go-download

A go cli tool to download a file or folder over the wire using go



# API

`cli [url] [path] [..options..]`


- First arg is the URL to fetch
- Second arg is the path to download the file or folder at

Options:

- `--name` - Extra option to rename the download file or folder.